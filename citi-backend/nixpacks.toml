[phases.install]
cmds = [
  "curl -sS https://bootstrap.pypa.io/get-pip.py | python",
  "python -m pip install -r requirements.txt"
]

[start]
cmd = "python manage.py migrate && python manage.py seed_demo && daphne config.asgi:application --port $PORT --bind 0.0.0.0"